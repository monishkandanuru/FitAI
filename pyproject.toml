[project]
name = "fitai"
version = "1.1.0"
requires-python = ">=3.12,<3.14"

[tool.vercel]
entrypoint = "app:app"

[tool.vercel.scripts]
build = "python build.py"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
